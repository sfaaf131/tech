{
  "name": "kondax.tech",
  "install": "python3 -c \"from pathlib import Path; assert Path('index.html').is_file(), 'missing index.html'\"",
  "terminals": [
    {
      "name": "site",
      "command": "python3 -m http.server 3000 --bind 0.0.0.0",
      "description": "Serves the static holding page on port 3000"
    }
  ],
  "ports": [
    {
      "name": "site",
      "port": 3000
    }
  ]
}
