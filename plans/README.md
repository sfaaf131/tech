# Implementation Plans

Generated by the improve skill on 2026-08-23. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

Planned at commit `8001878`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Keep the cooperate note after "success" | P1 | S | — | DONE |
| 002  | Stop rejecting real notes that start with a greeting | P1 | S | — | DONE |
| 003  | Extend check.ts over pages, related slugs, and dates | P1 | S | — | DONE |
| 004  | Gate the Entrar CTA on isOpenExperimentSlug | P2 | S | 003 | DONE |
| 005  | Set the timing token on mount; tolerate small clock skew | P2 | S | 001 | DONE |

## Dependency notes

- 004 should land after 003 so a paused/closed + `openToJoin` mismatch fails CI instead of only the UI.
- 005 should land with or after 001: if success still unmounts the form, a silent 200 from an empty `t` still destroys the note.
- 001, 002, and 003 are independent of each other.

## Findings considered and rejected

- Durable/KV rate limit: the store does not persist; AGENTS.md forbids DATABASE_URL theater.
- Rate-limit invalid bodies: AGENTS.md says the bucket increments only after a valid parse.
- Persist the form / Prisma / auth / factory copy: product constraints.
- CSP, COOP/CORP, HTTPS-only `link`: hardening without a current sink.
- Hydrate-less Header, pin all deps, RSS, intent-radio focus: lower leverage than 001–005.
